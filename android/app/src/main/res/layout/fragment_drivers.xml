<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/wtcs_background">

    <!-- BARRA DE BÚSQUEDA -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:cardBackgroundColor="@color/wtcs_surface"
        app:cardCornerRadius="8dp"
        app:cardElevation="4dp">

        <androidx.appcompat.widget.SearchView
            android:id="@+id/searchView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:iconifiedByDefault="false"
            app:queryHint="Search driver..."
            android:theme="@style/WhiteCursorSearchView" />
        <!-- Necesitaremos un pequeño estilo para que el texto sea blanco -->
    </androidx.cardview.widget.CardView>

    <!-- LISTA DE PILOTOS -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerDrivers"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="80dp"
        android:clipToPadding="false"/>

</LinearLayout>